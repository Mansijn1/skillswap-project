spring.application.name=skillswap
# Server settings
server.port=8082

# Fix Connection Link Failure: Datasource settings for Aiven Cloud MySQL
spring.datasource.url=jdbc:mysql://mysql-311fcdb1-mansijain5475-f31d.h.aivencloud.com:11018/defaultdb?useSSL=true&enabledTLSProtocols=TLSv1.2,TLSv1.3&allowPublicKeyRetrieval=true
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_GH_2uS-7wsQ_mKusya1
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Thymeleaf settings (Disable cache for dev, enable debug)
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# Logging: Show template errors in detail
logging.level.org.thymeleaf=DEBUG
logging.level.org.springframework.web=DEBUG

# Upload directory (Explicit relative path)
app.upload.dir=./uploads/

# Serve static files from uploads (Use the variable correctly)
spring.web.resources.static-locations=classpath:/static/,file:${app.upload.dir}

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB